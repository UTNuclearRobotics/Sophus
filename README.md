# Forked Sophus
This repo was forked from [Sophus](https://github.com/strasdat/Sophus) to change the minimum CMake requirement to `3.22`. It is a temporary fix to be built inside of a Docker container.